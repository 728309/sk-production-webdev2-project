function loadAndDisplayMixes() {
  // Assignment 2 - display mixes
  // fetch mixes from the API and display them inside the `<div id="mixes-container"></div>` element
}

function setupMixForm() {
  // Assignment 3 - create a mix
  // get reference to mix form and set up event listener
  // on submit, prevent default behavior, get form data, JSON encode it, and send POST request to API
}

window.addEventListener("DOMContentLoaded", () => {
  loadAndDisplayMixes();
  setupMixForm();
});
