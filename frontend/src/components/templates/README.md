# Templates

Templates arrange reusable sections into a full page layout.

Current example:

- `MixArchive`

Templates should receive data and callbacks through props and events. This keeps API loading logic in the route page while the template handles layout.
